setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
    - plan: heroku-redis
      as: REDIS
  config:
    env_file: .env.heroku
build:
  docker:
    web: Dockerfile
run:
  web: gunicorn EDSiteProject.wsgi:application
